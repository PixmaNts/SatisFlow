{
  "name": "satisflow-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend for SatisFlow factory tracking application",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-wasm": "cd ../crates/satisflow-wasm && wasm-pack build --target web --out-dir ../../frontend/src/wasm",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0",
    "pinia": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vite-plugin-wasm": "^3.2.0"
  }
}